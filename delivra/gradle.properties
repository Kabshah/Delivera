org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# NOTE: machine-specific java home lives in ~/.gradle/gradle.properties (NOT here —
# this file is committed and CI/Linux would choke on a Windows path).
